{
    "name": "techandbusinessorg-root",
    "private": true,
    "scripts": {
        "build": "cd astro-frontend && npm install && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}